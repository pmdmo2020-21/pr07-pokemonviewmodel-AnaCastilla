<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.winner.WinnerActivity">

    <TextView
        android:id="@id/txtWinnerTop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/txtWinnerTop"
        android:layout_marginTop="@dimen/winnerMargin"
        android:textSize="@dimen/textPokemon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <ImageView
        android:id="@id/imgWinnerPokemon"
        android:layout_width="0dp"
        android:layout_height="@dimen/sizeWinner"
        app:layout_constraintBottom_toTopOf="@id/txtWinnerBottom"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/txtWinnerTop"
        app:srcCompat="@drawable/pikachu"/>

    <TextView
        android:id="@id/txtWinnerBottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/txtPikachu"
        android:layout_marginBottom="@dimen/winnerMarginBottom"
        android:textSize="@dimen/textPokemonWinner"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>